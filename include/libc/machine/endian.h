/*
 * zedBSD
 * Copyright (C) 2026 Awe Morris
 *
 * SPDX-License-Identifier: Zlib
 */

/*
 * The byte order, under the name the BSDs give it.
 *
 * Portable software asks for <machine/endian.h> when it has not recognised
 * the system, because that is where a BSD keeps the byte order.  Nothing is
 * declared here that <endian.h> does not already declare: this is the same
 * header under the name such software knows to look for.
 */

#ifndef LIBC_MACHINE_ENDIAN_H
#define LIBC_MACHINE_ENDIAN_H

#include <endian.h>

#endif
