<!-- awesome-plan project=zedbsd record=future-work -->

# Future Work

現在の Priority と Queue から外した将来の項目。着手対象として選び直した時点で、新しい WS か Phase として計画を立てる。
Future Work は実行を許可しない。

| ID | 項目 | 価値・内容 | 由来 | 状態 | 再検討のきっかけ | 詳細 |
| --- | --- | --- | --- | --- | --- | --- |
| F-001 | bridge | L2 転送、member の所有、設定と永続化を独立して設計する。VLAN は含めない（2026-09-09 に取消し） | 旧 ws011-p004 | deferred | 複数 interface の L2 接続の要求 | [WS011](ws011/ws.md) |
| F-002 | CPAR の残り | runtime の名前空間と隔離、CLI・build、service container。完了済みの boot 設定基盤（p002〜p006）は現行機能として保つ | WS013 | deferred | container 分割・用途別構成（MG007）の具体化 | [WS013](ws013/ws.md) |
| F-003 | μITRON リアルタイム領域 | 互換 profile、RT と POSIX の境界、常駐実行、通信・障害・時間の保証の設計 | WS015 | deferred | リアルタイム用途の要求 | [WS015](ws015/ws.md) |
| F-004 | 旧 WS003 の未完了 | ws003-p001（Latitude 5320 の inventory）、p003（Latitude の xHCI の初期化）、p017（UEFI LoadOptions）、p025（HAL の clock source の分割と amd64 SMP の単調 counter）。インストーラの実機動作（旧 p018・p019・p026〜p032）は WS028 へ引き継いだ（[内容](ws028/inherited-ws003.md)）、PowerPC（旧 p033〜p039）は WS027 へ移した | WS003 の終了（2026-09-12 ユーザー指示） | deferred | 該当機種・機能の具体的な要求。再開するときは WS003 を使わず新しい WS を立てる | [WS003](ws003/ws.md) |
| F-005 | `[` の実行 file | `test` と同じ program を `[` の名前で `/bin` に置く（`find -exec [ ...` などのため）。package 名がそのまま install 名なので、install 名を別に持てる仕組みが要る | ws043-p009 | deferred | WS047 の build system で install 名を持てるようになったとき、または `[` を外から呼ぶ script が見つかったとき | [WS043](ws043/ws.md)（ws043-p009） |
| F-006 | sh の vi mode の残りの命令 | 履歴の検索（`/`・`?`・`n`・`N`）、文字への移動（`f F t T ; ,`）、`.`（繰り返し）、`v`（editor で編集）、補完（`=`・`\`・`*`）、`_`、挿入 mode の Ctrl-V。XCU sh の vi-mode の一覧のうち ws042-p008 で実装しなかったもの | ws042-p008 | deferred | 対話の利用でこれらが要ると分かったとき、または POSIX の vi-mode の完全さを受け入れ条件にする WS | [WS042](ws042/ws.md)（ws042-p008） |
| F-007 | make の範囲外の機能 | `GNUmakefile` を読むこと（gnulib の maint.mk が GNU make の全体を求める）、`-j` と jobserver、`.ONESHELL`、`.SECONDEXPANSION`、grouped target（`&:`）、`--output-sync`、関数 `file`・`let`・`intcmp`、archive の member（`lib.a(m.o)`、POSIX にはある） | ws046-p001 | deferred | それを使う package を build するとき（archive の member は POSIX の完全さを受け入れ条件にするとき） | [WS046](ws046/ws.md)（ws046-p001） |
| F-008 | libtool が zedbsd の共有 library を作れるようにする | autotools の package の libtool（`libtool.m4`）は `x86_64-unknown-zedbsd` を知らず（dynamic linker characteristics... no）、共有 library を作らず static だけにする。expat の install に `libexpat.so*` が無い。package の patch（`libtool.m4` の host の表に zedbsd を足す）か、configure の cache 変数で教える | ws046-p008 | deferred | 共有 library を要る package を guest で build するとき、または WS047 の build system | [WS046](ws046/ws.md)（ws046-p008） |
| F-009 | ld.lld の `--no-undefined-version` の既定 | guest の ld.lld は version script に書かれた未定義の symbol を error にする（lld 16 からの既定）。GNU ld は通すので、zlib の configure の共有 library の検査（1 関数の object を zlib.map と link）が落ち、static だけになる。`LDFLAGS=-Wl,--undefined-version` で共有 library も作れ、試験も通り、install の一覧が host と一致した。zedbsd の clang の driver の既定にするか、package ごとに与えるかを決める | ws046-p008 | promoted（2026-09-24 ユーザー決定: clang の既定にする → WS055） | 共有の zlib が要るとき、または toolchain（WS036）の driver の既定を見直すとき | [WS046](ws046/ws.md)（ws046-p008） |
| F-010 | UFS の directory を間接の block まで育てる | WS054 は直接の 12 block（bsize 8192 で 96 KiB、約 3000 の entry）まで。それを超える directory に名前を足すと ENOSPC（読むのは間接の block もできる） | ws054-p001 | deferred | 1 つの directory に数千を超える file を置く用途が出たとき | [WS054](ws054/ws.md)（ws054-p001） |
| F-011 | `ARG_MAX` を大きくする | guest の `getconf ARG_MAX` は 16384（POSIX の最小 4096 は満たす。Linux は 2 MiB）。1500 の file の `rm dir/*` が `Argument list too long` になった。package の build の `rm *.o` や長い link の command で当たりうる | ws054-p002 | deferred | 実 package の build で E2BIG が出たとき | [WS054](ws054/ws.md)（ws054-p002） |
| F-012 | guest の LLVM package を libc・uapi header の変更で全 rebuild しない | `limits.h` 1 行や libc の 1 関数の変更で guest の disk image の build が LLVM の 5717 object を作り直す（約 15 分、64 core）。package は sysroot 全体に依存しているため。header の内容の hash や sysroot の部分の stamp に依存を細かくする | ws056-p001 | deferred | guest の image の build の待ちが Phase の回帰の大半になったとき | [WS047](ws047/ws.md)（build system の設計）の候補 |
| F-013 | inode・file・overlay の表を動的確保と hash に | `INODE_COMMON_MAX`・`FILE_MAX`・`OVERLAY_INODE_MAX` は `.vfs_bss` の静的配列（entry は 248〜792 B）で、overlay の identity の探索は線形（lookup ごとに全 slot、subtree の rename は 2 乗）。hash と動的確保にすれば上限を物理に比例させられる。ws058-p002 は 2048 に留めた | ws058-p002 | deferred | 現代向けの上限（ws058-p002 の値）でも表が足りないとき、または 512 MiB の機械で `.bss` が問題になったとき | [WS058](ws058/ws.md) |
| F-014 | UEFI・BIOS の loader の `zedbsd.cfg` の探索順と、UFS からの直接 load | 2026-09-25 ユーザー: `zedbsd.cfg` は ESP を先に探し、無ければ BOOT という名前の FAT partition を探す（UEFI only・UEFI+BIOS・BIOS only で整合する。UEFI+BIOS でも ESP は FAT なので読める）。vmunix は 2 番目の partition（UFS の root）に置くのが正しく、vmunix も loopback の image も UFS から直接 load できるようにする。今の native の layout（WS062）は vmunix を ESP に置く暫定 | ユーザー指示（ws062-p002 の途中） | deferred | WS062 の後。ユーザー「記録だけして後回し」 | [WS062](ws062/ws.md) |
| F-015 | UFS の書き込みを delayed write に（flusher）と、順序のための device flush の削減 | UFS の書き込みは全部 write-through で、順序のために `disk_sync`（buf_sync + `bio_flush`）を 11 か所で呼ぶ。configure（/root）は tmpfs より 6 秒遅い（system +1.4 秒、残りは flush の待ち）。buffer cache は dirty list と `buf_writeback` を持つので、flusher（数秒ごと、fsync・journal は今まで通り同期）を足せば delayed write にできる。耐久性の方針（電源断で失う範囲）はユーザーの判断 | ws062-p002 | promoted（2026-09-26 ユーザー「write cached をデフォルト、write thru はマウントオプション」→ [ws061-p006](ws061/phase006/phase.md)） | p005 の後に configure（/root）が host の 2 倍を超えていて、UFS の差が残っているとき | [WS061](ws061/ws.md) |
| F-016 | base の make の並列（`-j`） | base の make は `-j` を受け付けて無視する設計（`userland/base/make/main.c`）で、`make -j4` も直列。expat の make は guest 24.6 秒（UFS）に対し host は `-j1` 15.2 秒・`-j4` 5.2 秒。並列の job の管理（jobserver を含む）を足せば 4 vCPU を使える。kernel の側は p005 で CPU の間の移動を直した | ws061-p005 | promoted（2026-09-26 ユーザー「makeは-jに対応させて、並列makeの実行時間もホストと同等以上に」→ [WS064](ws064/ws.md)） | 並列の build の時間が問題になったとき | [WS064](ws064/ws.md) |
| F-017 | loader の残りの費用（SysV hash の実行 file、重複の探索、lazy binding） | ws061-p009 の後も `cc` の loop の guest の CPU の 32% が `/lib/ld.so`。clang の driver が実行 file を `--hash-style=sysv` で link するので、どの探索も最初の object（実行 file）で SysV の hash を要る。同じ symbol を指す再配置（libLLVM で 5828 件中 4211 種）の結果の cache、`--hash-style=both` を driver の既定に（cross の toolchain と guest の LLVM の rebuild）、`-z now` を外した lazy binding が候補 | ws061-p009 | promoted（2026-09-26 ユーザー「ld.soの最適化をあとでやるリストとしてwsにしておこう」→ [WS066](ws066/ws.md)） | `cc` や動的な program の起動が再び host より遅くなったとき | [WS066](ws066/ws.md) |
| F-018 | sh の配列（`a=(...)`、`${a[i]}`、`"${a[@]}"`、`declare -a`・`-A`） | POSIX では構文の誤りなので足しても逸脱にならないが、変数の表・展開・`declare`・`local`・`unset`・`read -a` に広く及ぶ。ws065-p003 の `declare -a`・`-A` は「not supported」で status 2 | WS065 の判断の表（2026-09-26） | deferred | 配列を使う `/bin/sh` script を動かす要が出たとき | [WS065](ws065/ws.md) |
| F-019 | sh の残りの bash 互換（`${x@Q}` などの `@` 変換、`kill -l`・`-L` の不正な引数の後も続ける、`set` の出力の引用の形、`.` の not found の status） | ws065-p003 の dash・bash との差分で見えた。`${x@Q}` は POSIX では bad substitution、`kill -l 10 BAD 12` は bash が残りも出して status 1、我々は止めて 2。`set` の引用と `.` の status 2 は dash と同じにしてある | ws065-p003 | deferred | これを使う script が出たとき | [WS065](ws065/ws.md) |
| F-020 | base の program の静的 link、make の単純な行の直接実行 | 2026-09-26 ユーザーの問い「sh,echo,printfなど基盤的なツールをスタティックリンクにしたらmakeが爆速になったりする？」で測った（guest、QEMU 8 GiB 4 vCPU NVMe）: `sh -c :` の起動は動的 1.0 ms、静的 0.72 ms（1 回あたり 0.28 ms、ld.so と libc.so の再配置）。expat の configure 7.15・7.50 → 7.09・7.22 秒、`make -j1` 10.36・10.10 → 10.16・9.94 秒、`make -j4` 4.08・4.00 → 4.05・4.06 秒で、差は 0〜3%（ばらつきの内）。時間の大半は clang の compile と libtool（shell script）の解釈で、起動ではない。`echo`・`printf` は既に sh の builtin（pipe の中も ws064-p004 で fork なし）。静的にするなら全 program の起動を縮める ld.so の側（F-017）の方が効く。GNU make のように metacharacter の無い行を shell を通さずに実行すれば、行ごとに sh の起動（約 1 ms）が減る | ユーザーの問い（ws065-p003 の途中） | 静的 link は採らない（2026-09-26 ユーザー「とりあえずダイナミックリンクに戻して」。loader の側は [WS066](ws066/ws.md)）。make の直接実行は deferred | build の時間の内訳で process の起動が大きくなったとき | [WS066](ws066/ws.md)・[WS064](ws064/ws.md) |
| F-021 | Venus（QEMU）でのウィンドウモードの frame の時間 | ws035-p052 の測定で、1280×800 の合成の 1 frame が約 100 ms（swapchain の acquire 21〜23 ms、`vkQueueSubmit`+`vkQueuePresentKHR` 45〜73 ms、記録ほか約 20 ms）。host の Lavapipe で描く環境で vkdemo も 8 fps 程度。libvulkan の display の WSI の present（同期の待ち、blob の scanout）と Venus の命令の往復の費用を分けて測り、減らせるか調べる | ws035-p052 | deferred | 実機（i915）でウィンドウモードが 60 Hz に届かないとき、または QEMU でのデモが遅すぎるとき | [WS035](ws035/ws.md) |
| F-022 | i915 のネイティブ実行器で、別の process（context）の GPU の画像を import して sampling する | Wiseman Mode（`zwl --glass`）は i915 で wl_shm の窓までは合成できる（ws035-p066）が、Vulkan の client（mview・wltest）の buffer を zwl が import して描くと、実行器が import の宣言を無視し（`render/memory.c` の XXX「no sharing between contexts」）、別の surface へ copy して device lost になる。`GPU_CAP_SHARE` の画像の import を memory の storage として扱い、GPU の client の窓を Wiseman Mode で合成できるようにする | ws035-p066 | promoted（ws035-p067 cleared、2026-09-26） | Wiseman Mode で GPU の client（mview 等）を i915 実機で出すとき（OSC のデモ） | [phase066](ws035/phase066/phase.md)、[phase067](ws035/phase067/phase.md)、WS031 |
| F-023 | i915 のネイティブ実行器・compiler の不足の補い | ws035-p066 で zwl 側で避けたもの: `gl_VertexIndex`・`gl_FragCoord` の builtin、OpSwitch（整数の比較の連鎖、merge-return）、OpCompositeInsert、triangle strip（topology 4）、vkFreeDescriptorSets（opcode 78）、kernel の fence（`GPU_CAP_FENCE`）と allocation の共有（`GPU_CAP_ALLOCATION_SHARE`）。一般の Vulkan アプリはこれらを使う | ws035-p066 | deferred | i915 で一般の Vulkan アプリ・toolkit を動かすとき | [phase066](ws035/phase066/phase.md)、WS031 |
