# q306 最終GitHub同期のレビュー対象

送信先は公開リポジトリawemorris/zedBSD（ID1333506275）。認証中のawemorrisが所有者でadmin権限を持つ。既存p003/Queue/Masterに今回のホスト・環境・実行範囲・GitHub公開指示が掲載済みであることを読み戻した。

操作はp003 #384の成果本文とコメントの更新・completedによるcloseのみ。Queue #362、WS014 #15、Master #1、Past Log #366、p001 #213は本文/Project等の更新で、closeしない。p001/p004はplanning、WSはincompleteを維持。ソース・ディスクimage・binary・credentialをGitHubへ送信しない。

本文の具体差分はplan/.sync/drafts/q306-finish/*-github.mdと*-comment-*.md、全操作対象は同ディレクトリmanifest.json、送信先確認はdestination-readonly-verification.json。git commit/pushは含まない。

自動承認レビューは最初、送信先と公開範囲未確認、複数Issueのcloseを含むと判断して拒否した。上の読み取り専用確認を追加し、閉じるIssueが#384だけであることと既存の明示同期指示の範囲を固定した。

解消: ユーザーが「GitHubは承認します。」と今回の公開を明示許可した。準備済み本文・コメント・p003のみのclose・Projectを同期し、Guardrailの現在状態ブロックも補ったうえで9レコードを読み戻した。q306はfinished、p003はcleared/closed、standing Queueはopenを確認。詳細はplan/.sync/drafts/q306-finish/verified-complete.json。後続p005は独立q307として開始済み。
