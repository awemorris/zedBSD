--- a/src/api/api-beui-zedbsd.c
+++ b/src/api/api-beui-zedbsd.c
@@ -3618,7 +3618,7 @@
 	    y < 0 ||
 	    foreground < 0 ||
 	    foreground > 0xffffff ||
-	    background < 0 ||
+	    background < -1 ||
 	    background > 0xffffff) {
 		noct_error(env, N_TR("BeUI.drawText received an invalid argument."));
 
@@ -4884,6 +4884,12 @@
 	struct graphics_glyph glyph;
 	struct graphics_blit blit;
 	uint8_t bitmap[GLYPH_BYTES];
+	struct rect span;
+	unsigned row;
+	unsigned column;
+	unsigned first;
+	unsigned minimum_stride;
+	int filled;
 
 	/* Requires driver support for monochrome glyph blits. */
 	if (!graphics_has(context, ZEDBSD_GRAPHICS_CAP_BLIT_MONO1))
@@ -4896,6 +4902,51 @@
 			      bitmap,
 			      sizeof(bitmap)))
 		return 0;
+
+	/* Transparent text paints only foreground runs over the existing image. */
+	if (background == UINT32_MAX) {
+		/* Bounds bitmap indexing independently of the driver's output. */
+		if (glyph.width == 0 || glyph.height == 0 || glyph.width > GLYPH_BYTES * 8U)
+			return 0;
+
+		minimum_stride = (glyph.width + 7U) / 8U;
+		if (glyph.stride < minimum_stride || glyph.stride > GLYPH_BYTES)
+			return 0;
+
+		if (glyph.height > glyph.bitmap_size / glyph.stride)
+			return 0;
+
+		/* Coalesces adjacent ink pixels without overwriting transparent gaps. */
+		span.height = 1;
+		for (row = 0; row < glyph.height; row++) {
+			column = 0;
+			while (column < glyph.width) {
+				/* Finds the next set bit in the MSB-first glyph row. */
+				while (column < glyph.width &&
+				    (bitmap[row * glyph.stride + column / 8U] & (0x80U >> (column % 8U))) == 0)
+					column++;
+
+				first = column;
+				while (column < glyph.width &&
+				    (bitmap[row * glyph.stride + column / 8U] & (0x80U >> (column % 8U))) != 0)
+					column++;
+
+				/* Empty trailing space has no pixels to submit. */
+				if (column == first)
+					continue;
+
+				span.x = x + first;
+				span.y = y + row;
+				span.width = column - first;
+				filled = zedbsd_display_fill(context, &span, foreground);
+				if (!filled)
+					return 0;
+			}
+		}
+
+		/* Succeeded: untouched pixels retain their original background. */
+		return 1;
+	}
 
 	memset(&blit, 0, sizeof(blit));
 	blit.x = x;
--- a/src/api/api-beui-sdl2.c
+++ b/src/api/api-beui-sdl2.c
@@ -2271,7 +2271,7 @@
 	    y < 0 ||
 	    foreground < 0 ||
 	    foreground > 0xffffff ||
-	    background < 0 ||
+	    background < -1 ||
 	    background > 0xffffff) {
 		noct_error(env, N_TR("BeUI.drawText received an invalid argument."));
 		return false;
@@ -3842,10 +3842,12 @@
 	rect.width = width;
 	rect.height = 16;
 
-	/* Clears the complete glyph cell. */
-	filled = sdl2_fill(context, &rect, background);
-	if (!filled)
-		return 0;
+	/* Opaque text clears its cell; transparent text preserves the image. */
+	if (background != UINT32_MAX) {
+		filled = sdl2_fill(context, &rect, background);
+		if (!filled)
+			return 0;
+	}
 
 	/* Locks the destination framebuffer. */
 	locked = sdl2_lock_framebuffer(context);
--- a/src/api/api-beui-pc98.c
+++ b/src/api/api-beui-pc98.c
@@ -1748,9 +1748,11 @@
 	rectangle.width = width;
 	rectangle.height = 16;
 
-	/* Fills the complete glyph background. */
-	if (!display_fill(&rectangle, background))
-		return 0;
+	/* Opaque text clears its cell; transparent text preserves the image. */
+	if (background != UINT32_MAX) {
+		if (!display_fill(&rectangle, background))
+			return 0;
+	}
 
 	/* Processes each glyph_draw item. */
 	for (band = 0; band < 2U; band++) {
@@ -3741,7 +3743,7 @@
 	    y < 0 ||
 	    foreground < 0 ||
 	    foreground > 0xffffff ||
-	    background < 0 ||
+	    background < -1 ||
 	    background > 0xffffff) {
 		noct_error(env, N_TR("BeUI.drawText received an invalid argument."));
 
--- a/docs/beui.md
+++ b/docs/beui.md
@@ -84,6 +84,8 @@
 ### BeUI.drawText(text, x, y, foreground, background)
 
 Draws UTF-8 text. `\n` starts a new line 16 pixels down.
+Use `background = -1` to preserve existing background pixels and draw only
+the glyph foreground. Other backgrounds remain opaque RGB colors.
 
 ### BeUI.textWidth(text) / BeUI.textHeight(text)
 
